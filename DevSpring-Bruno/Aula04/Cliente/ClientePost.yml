info:
  name: ClientePost
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:8080/aula04-cliente
  body:
    type: json
    data: |-
      {
        "nome": "teste",
        "email": "email@gmail.com"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
