info:
  name: UsuarioGetNomeCidade
  type: http
  seq: 2

http:
  method: GET
  url: http://localhost:8080/aula01-usuario/buscar?nome=Lucas&cidade=Torres
  params:
    - name: nome
      value: Lucas
      type: query
    - name: cidade
      value: Torres
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
