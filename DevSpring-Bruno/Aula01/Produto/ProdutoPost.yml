info:
  name: ProdutoPost
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:8080/aula01-produto
  body:
    type: json
    data: |-
      {
        "produto": "abacaxi"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
