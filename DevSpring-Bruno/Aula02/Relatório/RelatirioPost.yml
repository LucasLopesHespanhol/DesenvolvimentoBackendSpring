info:
  name: RelatirioPost
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:8080/aula02-relatorio
  body:
    type: json
    data: |-
      {
        "tipo": "tipo 01"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
