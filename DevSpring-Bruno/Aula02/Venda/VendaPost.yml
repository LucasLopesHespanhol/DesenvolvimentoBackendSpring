info:
  name: VendaPost
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:8080/aula02-venda
  body:
    type: json
    data: |-
      {
        "venda": "Venda 01"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
