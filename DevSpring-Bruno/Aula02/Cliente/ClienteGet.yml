info:
  name: ClienteGet
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:8080/aula02-cliente?nome=lucas&cidade=Torres
  params:
    - name: nome
      value: lucas
      type: query
    - name: cidade
      value: Torres
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
